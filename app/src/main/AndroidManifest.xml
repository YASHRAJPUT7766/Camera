<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        SAFETY NOTE (read before adding permissions):
        This app is a *visible*, consent-based camera security testing lab.
        Only request permissions that a normal, transparent camera app needs.
        Do NOT add: RECEIVE_BOOT_COMPLETED, SYSTEM_ALERT_WINDOW, BIND_ACCESSIBILITY_SERVICE,
        FOREGROUND_SERVICE_CAMERA used to hide capture, or any device-admin permission.
        The local streaming server only needs local network access, never internet
        server exposure beyond the LAN, and no INTERNET permission for uploading data offsite.
    -->

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CameraSecLab"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <!--
            usesCleartextTraffic + network_security_config restrict cleartext HTTP
            to the local streaming server on the LAN only (see res/xml/network_security_config.xml).
            This is a local test dashboard, not a public web service.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CameraSecLab"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            No <service> is declared. The local streaming server and camera pipeline
            run only while MainActivity is alive and only after the user explicitly
            starts them. This is intentional: it means streaming and the camera both
            stop when the app leaves the state a normal foreground app can hold,
            which is exactly what the Security Test screen is meant to demonstrate.
            No BroadcastReceiver, no boot listener, no accessibility service, no
            device admin receiver, no background camera access.
        -->

    </application>

</manifest>
