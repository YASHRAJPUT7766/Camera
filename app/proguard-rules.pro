# This is a testing-lab app; release builds are not obfuscated by default
# (isMinifyEnabled = false in app/build.gradle.kts) so the security-test
# behavior stays easy to inspect. If you enable minification, add rules
# here as needed.
